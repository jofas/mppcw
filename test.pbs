#!/bin/bash --login

# PBS job options (name, compute nodes, job time)
#PBS -N mpp_out
#PBS -l select=2
#PBS -l walltime=03:00:00
#PBS -A d171-s2000901
#PBS -l place=excl
#PBS -j oe

# Change to the directory that the job was submitted from
# (remember this should be on the /work filesystem)
cd $PBS_O_WORKDIR

mpiexec_mpt -n 2 ./percolate_par
